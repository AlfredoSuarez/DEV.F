var num = prompt("Por favor, elija un número");

if (num %2 == 0){
    document.write(num + " número es divisible entre 2");
} else{
    document.write(num + "no es un número divisible entre 2 de forma entera" );
}
